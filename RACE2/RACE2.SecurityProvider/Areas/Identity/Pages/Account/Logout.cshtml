@page
@model LogoutModel
@* @{
    ViewData["Title"] = "Log out";
}

<header>
    <h1>@ViewData["Title"]</h1>
    <br/>
    @{
        if (User.Identity.IsAuthenticated)
        {
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/", new { area = "" })" method="post">
                <button type="submit" class="govuk-button" style="background-color: #00703c;color:white">Confirm</button>
            </form>
        }
        else
        {
            <p><b>You have successfully logged out of the application.</b></p>
        }
      }
</header> *@
